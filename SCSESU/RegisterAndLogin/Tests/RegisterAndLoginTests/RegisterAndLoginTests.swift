import XCTest
@testable import RegisterAndLogin

final class RegisterAndLoginTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RegisterAndLogin().text, "Hello, World!")
    }
}
